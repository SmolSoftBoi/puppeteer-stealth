{
  "name": "puppeteer-stealth",
  "version": "0.0.1",
  "description": "For puppeteer to prevent detection.",
  "main": "dist/index.js",
  "repository": "https://github.com/EpicKris/puppeteer-stealth.git",
  "author": "Kristian Matthews <kristian.matthews@me.com>",
  "scripts": {
    "lint": "eslint src/**.ts --max-warnings=0",
    "build": "rimraf ./dist && tsc",
    "test": "jest --coverage",
    "test:watch": "jest --coverage --watch",
    "prepublishOnly": "npm run lint && npm run build"
  },
  "engines": {
    "node": ">=8"
  },
  "dependencies": {
    "puppeteer-extra-plugin-stealth": "^2.7.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@typescript-eslint/eslint-plugin": "^4.28.5",
    "@typescript-eslint/parser": "^4.28.5",
    "eslint": "^7.31.0",
    "jest": "^29.7.0",
    "puppeteer": "^13.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^4.3.5"
  }
}
